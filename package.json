{
  "scripts": {
    "postinstall": "cd client && yarn install",
    "lint": "cd client && yarn run lint",
    "lint:force": "cd client && yarn run lint:force",
    "lint:staged": "cd client && yarn run lint:staged",
    "format": "cd client && yarn run format",
    "precommit": "cd client && yarn run precommit"
  },
  "name": "hatch-site",
  "private": true,
  "dependencies": {},
  "devDependencies": {}
}
